#include <iostream>
#include <math.h>
#include <conio.h>
using namespace std;
// "editor.formatOnSave":true

main()
{
    int i, z, m[2];
    for (i = 0; i < 2; i++)
    {
        scanf("%d", &z);
        m[i] = z
    }
    for (i = 0; i < 10; i++)
        ;
    ptintf("m[%d]=%d\n", i, m[i]);
    getch()
}

// int main(){double n, summ=0, col, chislo, sra;
//     for (n=1; n<=5; n++){cin >> chislo;     summ+=chislo;}
//     sra = summ/5;
//     cout<< endl << sra << endl;
//     block;

int[0] = {z} int min = m[0];
for (int i = 0; i < 5; ++i)
{
    if (m[i] < min)
    {
        min = m[i];
    }
}

int m[0] = {z};
int max = m[];
for (int i = 0; i < 5; ++i)
{
    if (m[i] > max)
    {
        max = m[]
    }
}
}
